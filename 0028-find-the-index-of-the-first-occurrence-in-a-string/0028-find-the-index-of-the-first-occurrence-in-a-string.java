class Solution {
    public int strStr(String haystack, String needle) {
        // Use the built-in method indexOf to find the first occurrence of needle in haystack
        return haystack.indexOf(needle);
    }
}
